@{
    ViewData["Title"] = "Akses Ditolak";
    Layout = "_LayoutDashboard";
    var email = Context.Session.GetString("UserEmail") ?? "User";
    var role = Context.Session.GetString("UserRole") ?? "-";
}

<div class="max-w-4xl mx-auto">
    <!-- Header -->
    <div class="flex items-start justify-between gap-4 mb-6">
        <div>
            <div class="text-sm text-gray-500">Security</div>
            <h1 class="text-2xl font-bold text-gray-900">
                Kamu tidak memiliki akses 😬
            </h1>
            <p class="text-gray-600 mt-2">
                Kamu lagi coba buka halaman yang gak sesuai role / izin akunmu.
            </p>
        </div>

        <div class="shrink-0">
            <span class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-red-50 text-red-700 text-sm font-semibold border border-red-100">
                <span class="w-2 h-2 rounded-full bg-red-600"></span>
                403 Forbidden
            </span>
        </div>
    </div>

    <!-- Main Card -->
    <div class="bg-white rounded-2xl shadow p-6 border border-gray-100">
        <div class="flex flex-col md:flex-row md:items-center gap-6">
            <!-- Icon block -->
            <div class="flex items-center justify-center w-16 h-16 rounded-2xl bg-red-50 border border-red-100">
                <svg class="w-8 h-8 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                          d="M12 11c1.657 0 3-1.343 3-3S13.657 5 12 5s-3 1.343-3 3 1.343 3 3 3zm7 8a7 7 0 10-14 0h14z" />
                </svg>
            </div>

            <!-- Text -->
            <div class="flex-1">
                <div class="text-lg font-bold text-gray-900">Akses kamu dibatasi</div>
                <p class="text-gray-600 mt-1">
                    Akun <span class="font-semibold text-gray-800">@email</span> dengan role kamu saat ini
                    belum punya izin untuk halaman ini.
                </p>

                <!-- Tips -->
                <div class="mt-4 grid md:grid-cols-2 gap-3">
                    <div class="rounded-xl border border-gray-100 bg-gray-50 p-4">
                        <div class="font-semibold text-gray-800">Yang bisa kamu lakukan</div>
                        <ul class="mt-2 text-sm text-gray-600 space-y-1">
                            <li>• Balik ke dashboard</li>
                            <li>• Coba halaman yang sesuai role</li>
                            <li>• Minta admin ubah akses kalau perlu</li>
                        </ul>
                    </div>

                    <div class="rounded-xl border border-gray-100 bg-gray-50 p-4">
                        <div class="font-semibold text-gray-800">Kenapa ini terjadi?</div>
                        <p class="mt-2 text-sm text-gray-600">
                            Sistem ngejaga workflow biar tiap role cuma bisa akses step yang memang miliknya.
                        </p>
                    </div>
                </div>

                <!-- Actions -->
                <div class="mt-6 flex flex-wrap gap-3">
                    <a href="/Dashboard/Index"
                       class="inline-flex items-center justify-center px-4 py-2 rounded-lg bg-red-600 text-white font-semibold hover:bg-red-700 transition">
                        Kembali ke Dashboard
                    </a>

                    <a href="/"
                       class="inline-flex items-center justify-center px-4 py-2 rounded-lg bg-white text-gray-800 font-semibold border border-gray-200 hover:bg-gray-50 transition">
                        Ke Beranda
                    </a>

                    <form asp-controller="Auth" asp-action="Logout" method="post" class="inline">
                        @Html.AntiForgeryToken()
                        <button type="submit"
                                class="inline-flex items-center justify-center px-4 py-2 rounded-lg bg-white text-gray-800 font-semibold border border-gray-200 hover:bg-gray-50 transition">
                            Logout & Login Ulang
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer note -->
    <div class="mt-6 text-sm text-gray-500">
        Kalau ini harusnya boleh tapi kamu ditolak, berarti mapping role/route kamu belum pas — tinggal bilang admin/dev ya.
    </div>
</div>
